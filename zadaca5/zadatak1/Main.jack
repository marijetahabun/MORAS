class Main
{
	function void main()
	{
		var int i, n;
		var Array A;

		let n = 5;
		let A = Array.new(n);

		let i = 0;
		while(i<n)
		{
			do Output.printString("Broj: ");
			let A[i] = Keyboard.readInt("");
			let i = i + 1;
		}
		do QS.sort(A, 0, n);
		let i = 0;
		while(i<n)
		{
			do Output.printInt(A[i]);
			do Output.printString(" ");
			let i = i + 1;
		}
		return;
	}
}